## v0.7.4（2026-08-20）

### 新增
- **多预置配置指定默认项**：部署端提供多个预置配置时，可通过 `isDefault: true` 指定默认选中的配置；只有一个预置项时会自动作为默认项。
- **预置配置保护策略**：新增部署端策略开关。可通过 `LOCK_PRESET_CONFIG_PARAMS` 锁定除 API Key 外的预置参数及供应商定义，或通过 `PREVENT_PRESET_CONFIG_DELETION` 禁止删除预置配置与供应商。
- **预置配置 Markdown 说明**：预置配置可通过 `description` 字段提供 Markdown 说明，并在“当前配置”下方展示说明卡片。
- **sub2api 异步图片供应商**：新增内置 sub2api（异步）供应商，支持提交生成与编辑任务、轮询任务状态，并在连接中断或重启后继续恢复结果。

### 变更
- **API URL 结尾斜杠规则**：API URL 以 `/` 结尾时直接拼接接口，不补 `/v1` 前缀；未以 `/` 结尾时保持原有补齐规则。
- **JSON 预置配置支持多类型与智能覆盖**：JSON 预置支持混合配置 OpenAI 兼容、fal.ai 和自定义供应商。配置通过固定 ID 匹配已有项：除 API Key 始终保留本地值外，部署值与上次部署快照不同时覆盖一次，之后保留用户的本地修改，直到部署端再次更新对应字段；被部署端移除的预置会自动转为普通配置。
- **预置配置文件支持打包内嵌**：使用 `.json` 文件作为预置配置时，在构建或 Docker 启动时直接内嵌到页面中，浏览器无需访问原始配置地址。
- **“仅展示默认配置”升级为“仅使用预置配置”**：开启后只允许使用部署端提供的预置配置，禁止新建、删除或管理自定义供应商，但支持在多个预置配置间切换。
- **配置分享链接携带 ID**：分享链接包含配置 ID，再次打开时直接更新对应配置，支持在多配置场景下指定目标项。
- **自定义服务商支持 Codex CLI 模式**：自定义服务商可开启 Codex CLI 模式并应用对应的参数转换；同步接口在 Codex CLI 模式下生成多图时会自动拆分为并发单图请求。

### 修复
- **流式 Images API 图片下载**：修复流式事件返回图片 URL 时偶现无法识别的问题，并增强对流中心跳和命名事件的兼容。
- **Agent API 配置下拉筛选**：Agent 独立配置下拉框过滤掉缺少 API Key 或 API URL 的无效项。
- **并发请求保留部分成功结果**：同步自定义服务商并发请求部分失败或超时时，已成功生成的图片正常保留。
- **异步 Manifest 导入校验**：提交映射包含 `taskIdPath` 但缺少 `poll` 时直接拦截，避免导入无法轮询的配置。
